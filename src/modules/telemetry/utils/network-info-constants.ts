/**
 * Shared constants for network operations
 */
export const NETWORK_CONSTANTS = {
  // Max retry attempts for network operations
  MAX_RETRIES: 3,
  RETRY_DELAY: 1000, // 1 second
  
  // HTTP request timeout in milliseconds
  HTTP_TIMEOUT: 5000
};
